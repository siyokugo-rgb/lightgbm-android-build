# PROVISIONAL Python dependencies for the NAR data/training tooling under tools/.
#
# These versions were resolved and functionally validated in the Cloud Agent
# development environment (all tools/test_*.py pass; model.txt output verified
# loadable+predict-parity on the Android arm64 native LightGBM). They are NOT
# confirmed as the project's authoritative training environment.
#
# The authoritative, reproducible training environment is expected to be pinned
# by ALL THREE of: nar-v3-training-requirements.lock, wheelhouse-v3/, and
# wheelhouse-v3-sha256.csv. The authoritative offline path requires all three
# together (any missing asset is fail-closed). The real schema of
# wheelhouse-v3-sha256.csv has not been confirmed, so its verification is not yet
# implemented; until then .cursor/install.sh keeps the authoritative path
# fail-closed and uses this provisional file instead.
lightgbm==4.7.0
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.0
